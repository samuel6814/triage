{
  "name": "curatio-server",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "predev": "fuser -k 8001/tcp 5000/tcp 2>/dev/null || true",
    "dev": "concurrently -n api,ml -c green,blue \"nodemon index.js\" \"cd ml && ../.venv/bin/python -m uvicorn app:app --reload --host 127.0.0.1 --port 8001\"",
    "ml": "cd ml && ../.venv/bin/python -m uvicorn app:app --host 127.0.0.1 --port 8001"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.14"
  }
}
