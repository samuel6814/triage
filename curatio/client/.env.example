# Base origin of the deployed Curatio API (Express on Render).
# Leave empty for local dev so requests stay relative and use the Vite proxy.
# Example (production): https://curatio-api.onrender.com
VITE_API_URL=
